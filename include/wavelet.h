#ifndef __WAVELET_H__
#define __WAVELET_H__

// Substituir por uma classe !!!!

#include <cmath>

float * haar1d(float *input, int size);
float ** haar2d(float **input, int rows, int cols);

#endif // __WAVELET_H__

